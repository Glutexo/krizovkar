{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/Glutexo/krizovkar/master/src/krizovkar/schemas/template-v1.schema.json",
  "title": "Šablona křížovky Křížovkáře, verze 1",
  "description": "Strukturální mezivýsledek s rolemi buněk, sloty hesel a volitelně připravenou tajenkou.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "format",
    "kind",
    "version",
    "grid",
    "slots"
  ],
  "properties": {
    "format": {
      "description": "Jednoznačný název formátu.",
      "const": "krizovkar"
    },
    "kind": {
      "description": "Druh dokumentu.",
      "const": "template"
    },
    "version": {
      "description": "Hlavní verze datového modelu šablony.",
      "const": 1
    },
    "grid": {
      "description": "Obdélníková matice rolí všech buněk šablony.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "width",
        "height",
        "cells"
      ],
      "properties": {
        "width": {
          "type": "integer",
          "minimum": 1
        },
        "height": {
          "type": "integer",
          "minimum": 1
        },
        "cells": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "array",
            "minItems": 1,
            "items": {
              "$ref": "#/$defs/templateCell"
            }
          }
        }
      }
    },
    "slots": {
      "description": "Místa pro budoucí hesla v určeném pořadí.",
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/wordSlot"
      }
    },
    "secrets": {
      "description": "Volitelné tajenky rezervované v jednom nebo více slotech.",
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/templateSecret"
      }
    }
  },
  "$defs": {
    "coordinate": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "row",
        "column"
      ],
      "properties": {
        "row": {
          "type": "integer",
          "minimum": 1
        },
        "column": {
          "type": "integer",
          "minimum": 1
        }
      }
    },
    "templateCell": {
      "description": "Nevyplněné písmeno, budoucí legenda nebo nevyplňovaná buňka.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "type"
      ],
      "properties": {
        "type": {
          "enum": [
            "letter",
            "legend",
            "empty"
          ]
        }
      }
    },
    "wordSlot": {
      "description": "Souvislé místo pro jedno heslo.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "start",
        "direction",
        "length"
      ],
      "properties": {
        "id": {
          "description": "Stabilní identifikátor slotu v rámci šablony.",
          "type": "string",
          "pattern": "^[a-z][a-z0-9_-]*$"
        },
        "start": {
          "$ref": "#/$defs/coordinate"
        },
        "direction": {
          "enum": [
            "horizontal",
            "vertical"
          ]
        },
        "length": {
          "description": "Počet písmenných buněk; CH zabírá jednu.",
          "type": "integer",
          "minimum": 1
        },
        "legend": {
          "description": "Volitelná vepsaná legenda bezprostředně před začátkem slotu.",
          "$ref": "#/$defs/coordinate"
        }
      }
    },
    "secretPrompt": {
      "description": "Text zadání tajenky a jeho umístění u budoucí mřížky.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "text"
      ],
      "properties": {
        "text": {
          "type": "string",
          "minLength": 1,
          "pattern": "\\S"
        },
        "placement": {
          "enum": [
            "above",
            "below"
          ],
          "default": "above"
        },
        "alignment": {
          "enum": [
            "left",
            "right"
          ],
          "default": "left"
        }
      }
    },
    "templateSecretPart": {
      "description": "Jedna část tajenky přiřazená slotu.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "slot"
      ],
      "properties": {
        "slot": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9_-]*$"
        },
        "word_count": {
          "description": "Počet po sobě jdoucích slov známé tajenky v této části.",
          "type": "integer",
          "minimum": 1
        }
      }
    },
    "templateSecret": {
      "description": "Připravená tajenka s volitelně již známými slovy.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "parts"
      ],
      "properties": {
        "words": {
          "description": "Slova tajenky bez mezer a interpunkce; seznam zachovává povolené švy.",
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "pattern": "^[A-ZÁÄČĎÉĚÍŇÓÔÖŘŠŤÚŮÜÝŽ]+$"
          }
        },
        "parts": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/templateSecretPart"
          }
        },
        "prompt": {
          "$ref": "#/$defs/secretPrompt"
        }
      }
    }
  }
}
